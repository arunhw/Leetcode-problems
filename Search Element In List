#include <stdio.h>
int main() {
    int n, i, search, found = 0;
    scanf("%d", &n);
    int arr[n]; 
    for(i = 0; i < n; i++) {
        scanf("%d", &arr[i]);
    }
    scanf("%d", &search);
    for(i = 0; i < n; i++) {
        if(arr[i] == search) {
            found = 1;
            printf("Found");
            break;
        }
    }
    if(!found) {
        printf("Not Found");
    }
    return 0;
}
